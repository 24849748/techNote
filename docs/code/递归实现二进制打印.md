
利用除二取余 函数调用栈实现倒叙打印

```c
void to_binary(unsigned long n)
{
    int r = n % 2;

    if (n >= 2)
        to_binary(n / 2);

    printf(0 == r ? '0' : '1');
}

int main(int argc, char *argv[])
{
    unsigned long number;

    to_binary(123);
    printf("\n");

    return 0;
}
```